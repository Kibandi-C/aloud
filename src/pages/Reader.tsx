/** Reader app stub — input tabs, reading pane and playback dock land in Stage 1. */
export default function Reader() {
  return (
    <section className="mx-auto w-full max-w-3xl px-4 py-16">
      <div className="rounded-card border border-line bg-shell-2 p-8 text-center">
        <h2 className="font-display text-2xl text-paper">Reader</h2>
        <p className="mt-3 text-muted">
          The reading pane and playback controls arrive in Stage 1.
        </p>
      </div>
    </section>
  );
}
